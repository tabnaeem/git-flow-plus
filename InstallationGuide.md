# Installation Guide

Moved to [Installation.md](Installation.md) (cross-platform overview)
and [WindowsInstallation.md](WindowsInstallation.md) (the Windows
installer in full) — both cover everything this file used to, plus the
`.exe`/`.deb`/`.rpm`/`.pkg` installers that didn't exist yet when this
file was written.
